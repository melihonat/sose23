SQL-Befehle für phpmyadmin-Datenbank "omemory":

CREATE TABLE IF NOT EXISTS Spieler (
    id INT AUTO_INCREMENT PRIMARY KEY,
    spielname VARCHAR(20),
    email VARCHAR(20),
    passwort VARCHAR(20),
    level INT
);

CREATE TABLE IF NOT EXISTS Spiel (
    einzeln BOOLEAN,
    spieltan DATETIME,
    dauer INT,
    verlauf VARCHAR(50),
    gewinner INT,
    initiator INT,
    mitspieler INT,
    FOREIGN KEY (gewinner) REFERENCES Spieler(id),
    FOREIGN KEY (initiator) REFERENCES Spieler(id),
    FOREIGN KEY (mitspieler) REFERENCES Spieler(id)
);

CREATE TABLE IF NOT EXISTS Karte (
    id INT AUTO_INCREMENT PRIMARY KEY,
    bild BLOB
);

CREATE TABLE IF NOT EXISTS Level (
    level INT PRIMARY KEY,
    anzahl_karten INT,
    spielZeit INT
);